[project]
name = "chatgpt-task"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "croniter>=6.2.2",
    "fastmcp>=3.2.4",
    "google-genai>=2.0.1",
    "mcp>=1.0.0",
    "psycopg2-binary>=2.9.12",
    "python-dotenv>=1.2.2",
    "sqlalchemy==2.0.36",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
